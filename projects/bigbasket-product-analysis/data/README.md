# Dataset required

Add the BigBasket source CSV to this folder and name it exactly:

`BigBasket Products.csv`

The notebook uses a relative path, so run it with this project folder as the working directory.

